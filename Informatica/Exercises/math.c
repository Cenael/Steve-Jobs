#include <stdio.h>
#include <math.h>

int main (){ 
int numero=9;  //se è costante si può dichiarare così
float radice;

radice=sqrt(numero); //sqrt: funzione per radice quadrata
printf("Il risultato /x8A: %f", radice);

return 0;
}